@using Modelo.Entidades
@model IEnumerable<Persona>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/_ViewImports.cshtml";
}

<h1>Lista de Personas</h1>
<p>A continuación se presenta la lista de Personas del Sistema de Aprobación de Becas</p>

<div class = "container">
    <div class="row">     
        <div class="col-sm-6">
            <h3>Personas</h3>
        </div>
        <div class="col-sm-6">
            <a class="btn alert-success d-block"
                asp-controller="Personas" asp-action="Create">
                Crear Nuevo Registro
            </a>
        </div>
    </div>

    @if (TempData["mensaje"] != null)
    {
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
            @TempData["mensaje"]
        <button type="button" class="close" data-dismiss="alert" aria-label="close">
            <span aria-hidden="true">
                &times;
            </span>
        </button>
    </div>
    }

    <div class = "row">
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <td>Id</td>
                    <td>Nombres</td>
                    <td>Apellidos</td>
                    <td>Cédula</td>
                    <td>Ciudad</td>
                    <td>Fecha de Nacimiento</td>
                    <td>Teléfono</td>
                </tr>
            </thead>

            <tbody>
                @foreach(Persona persona in Model)
                {
                    <tr>
                        <td>@persona.PersonaId</td>
                        <td>@persona.Nombres</td>
                        <td>@persona.Apellidos</td>
                        <td>@persona.Cedula</td>
                        <td>@persona.Ciudad</td>
                        <td>@persona.FechaNacimiento</td>
                        <td>@persona.Telefono</td>
                        <td>
                            <a class="btn btn-warning"
                            asp-controller="Personas" asp-action="Edit" asp-route-id="@persona.PersonaId">
                            Editar
                            </a>
                            <a class="btn btn-danger"
                            asp-controller="Personas" asp-action="Delete" asp-route-id="@persona.PersonaId">
                            Eliminar
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    </div>
</div>

